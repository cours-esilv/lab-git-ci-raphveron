# Switch to the feature/{name} branch (replace {name} with your actual name)
git checkout feature/adumbledore

# Add the changes to the staging area
git add questions.md

# Commit the changes with a meaningful message
git commit -m "Answered questions in questions.md"

# Push the changes to the remote repository
git push origin feature/adumbledore
